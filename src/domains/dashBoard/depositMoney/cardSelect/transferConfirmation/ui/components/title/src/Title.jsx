import '../styles/main.scss'

const Title = () => {
  const defaultTitle = 'title-transfer-amount'
  return (
    <div className={`${defaultTitle}`}>
      <h1>Revisá que está todo bien</h1>
    </div>
  )
}
export default Title
