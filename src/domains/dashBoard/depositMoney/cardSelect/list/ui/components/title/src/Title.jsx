import '../styles/main.scss'

const Title = () => {
  const defaultTitle = 'title-select-card'
  return (
    <div className={`${defaultTitle}`}>
      <h1>Seleccionar tarjeta</h1>
    </div>
  )
}
export default Title
