import '../styles/desktop.scss'

const DeleteCard = () => {
  const defaultDelete = 'delete-card'
  return (
    <div className={`${defaultDelete}`}>
      <p>Eliminar</p>
    </div>
  )
}
export default DeleteCard
